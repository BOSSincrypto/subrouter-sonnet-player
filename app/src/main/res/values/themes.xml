<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.SonnetPlayer" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/black</item>
        <item name="colorSecondaryVariant">@color/black</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- Player Activity Theme -->
    <style name="Theme.SonnetPlayer.Player" parent="Theme.SonnetPlayer">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- Player Button Style -->
    <style name="Widget.SonnetPlayer.PlayerButton" parent="Widget.AppCompat.ImageButton">
        <item name="android:background">@drawable/player_button_background</item>
        <item name="android:tint">@color/player_controls_tint</item>
        <item name="android:layout_width">@dimen/player_control_size</item>
        <item name="android:layout_height">@dimen/player_control_size</item>
    </style>
</resources>
